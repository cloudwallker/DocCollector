# DocCollector runtime dependencies
# Core (required)
PySide6>=6.6.0
PyMuPDF>=1.23.0
python-docx>=1.1.0

# Optional encoding-detection fallbacks.
# The decoder already handles UTF-8/UTF-16 (BOM), GB18030 and rejects binary
# without them; these two only improve detection of rarer legacy encodings.
charset-normalizer>=3.0.0
chardet>=5.0.0
